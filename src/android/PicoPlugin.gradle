allprojects {
  repositories {
    flatDir {
      dirs 'libs'
    }
  }
}

dependencies {
  implementation(name:'PicoIO-v2', ext:'aar')
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}